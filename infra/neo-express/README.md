# Neo Express (Local Chain)

This folder will hold the local Neo N3 chain config used for integration tests.

Current repo config lives in `deploy/config/default.neo-express` and will be migrated here.

