package storage

import "testing"

func TestInterfacesExist(t *testing.T) {
	// The interfaces are consumed across the application. This test simply
	// ensures the package is recognised by go test.
}
