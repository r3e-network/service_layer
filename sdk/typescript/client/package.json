{
  "name": "@service-layer/client",
  "version": "0.1.0",
  "description": "Service Layer HTTP client SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "node --test dist"
  },
  "keywords": ["service-layer", "sdk", "client"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
