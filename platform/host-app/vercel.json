{
  "framework": "nextjs",
  "buildCommand": "pnpm install && pnpm turbo build --filter=meshminiapp-host",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "scope": "r3e-network",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
