# Supabase Edge

Edge functions will handle:

- authentication and user identity
- per-app/per-user limits (GAS only for payments, NEO only for governance)
- nonce / anti-replay
- routing to TEE services via mTLS

