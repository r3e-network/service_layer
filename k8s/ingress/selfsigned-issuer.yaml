# =============================================================================
# Self-signed ClusterIssuer for Local Development
# Used for generating TLS certificates for *.localhost domains
# =============================================================================
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
  labels:
    app.kubernetes.io/part-of: service-layer
spec:
  selfSigned: {}
